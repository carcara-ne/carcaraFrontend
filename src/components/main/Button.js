import styled from 'styled-components';

const Button = styled.button`
     background: #40e0d0;
     border-radius: 8px;
     color: white;
     width: 8rem;
     padding: 0.3rem 0;
     cursor: pointer;
    `;
    

export default Button;